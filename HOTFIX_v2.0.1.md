# 修复说明 - v2.0.1

## 🐛 修复的问题

**问题描述：**
节点界面上显示了重复的数值：
- `reserved: 1.0 1.0` 
- `min_safe_reserve: 2.0 2.0`

**原因：**
在 `INPUT_TYPES` 中使用了 `"display": "slider"` 属性，这在 ComfyUI 中会导致同时显示滑块值和输入框值，造成数值重复显示。

**解决方案：**
移除了 `reserved` 和 `min_safe_reserve` 参数中的 `"display": "slider"` 属性。

---

## 📝 具体修改

### 修改前：
```python
"reserved": ("FLOAT", {
    "default": DEFAULT_RESERVED_GB,
    "min": MIN_RESERVED_GB,
    "max": 32.0,
    "step": 0.1,
    "display": "slider",  # ❌ 导致重复显示
    "tooltip": "..."
}),
```

### 修改后：
```python
"reserved": ("FLOAT", {
    "default": DEFAULT_RESERVED_GB,
    "min": MIN_RESERVED_GB,
    "max": 32.0,
    "step": 0.1,
    # "display": "slider" 已移除 ✓
    "tooltip": "..."
}),
```

---

## ✅ 修复后效果

现在节点界面将正确显示：
- `reserved: 1.0` （单一数值）
- `min_safe_reserve: 2.0` （单一数值）

参数仍然可以通过拖动或输入来调整，功能完全不受影响。

---

## 🔄 更新方法

1. **删除旧版本**
   ```bash
   删除 ComfyUI/custom_nodes/ARES_GPU_Manager/
   ```

2. **解压新版本**
   ```bash
   解压 ARES_GPU_Manager_v2.0.1.zip
   放入 ComfyUI/custom_nodes/
   ```

3. **重启 ComfyUI**

或者直接替换 `nodes.py` 文件即可。

---

## 📊 版本信息

- **v2.0.0** - 初始改进版本
- **v2.0.1** - 修复界面显示问题（当前版本）

---

## 🙏 感谢

感谢用户反馈，帮助我们发现并修复了这个界面显示问题！

---

**最后更新：** 2025-01-30  
**状态：** ✅ 已修复
